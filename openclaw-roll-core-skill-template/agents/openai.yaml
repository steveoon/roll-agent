interface:
  display_name: "Roll Core"
  short_description: "Operate MCP agents through roll CLI"
  default_prompt: "Use $roll-core to check agent health, preserve routing keys, and call the right agent/tool through `roll run --json` or `roll ask --json`."
