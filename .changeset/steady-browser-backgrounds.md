---
"@roll-agent/browser": patch
"@roll-agent/browser-use-agent": patch
---

Keep managed CDP pages active when browser windows are occluded. Wait for the BOSS chat list after force reloads, restore the unread filter before subsequent batch reads, and include page visibility plus selected-chat diagnostics when synchronization times out.
