---
"@roll-agent/browser": patch
---

Allow clicking ordinary form inputs without requiring navigation capability, while retaining navigation checks for Enter submission, submit controls and links. Preserve native option values derived from their text when no explicit value attribute exists, so selection by value works consistently.
