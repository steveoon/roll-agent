---
"@roll-agent/core": minor
---

Improve `roll chat` prompt editing: use the full row before wrapping mixed-width drafts, keep the caret layout stable, navigate soft-wrapped rows with up/down, and recall up to 50 recent inputs from an empty prompt.
