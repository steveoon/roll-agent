---
"@roll-agent/browser": patch
"@roll-agent/browser-use-agent": patch
---

Fix the published `@roll-agent/browser` npm manifest to export `dist` files instead of `src` files, and republish `@roll-agent/browser-use-agent` against the fixed browser package.
