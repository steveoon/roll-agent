---
"@roll-agent/core": patch
---

Fix official `octopus-agent` handling by supporting legacy mapped `roll-env-file` declarations in `roll doctor` and resolving catalog installs by skill name such as `octopus-agent`.
