import type { AgentDefinition } from "./types/index.ts";

export function defineAgent(definition: AgentDefinition): AgentDefinition {
  // TODO: wrap with MCP Server setup + listen()
  return definition;
}
