/**
 * @deprecated Import Relay Wire schemas and types from `@roll-agent/relay-protocol`.
 * This facade remains for one migration cycle so existing Companion consumers keep
 * the same schema object identity.
 */
export * from "@roll-agent/relay-protocol";
